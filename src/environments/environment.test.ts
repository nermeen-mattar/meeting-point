export const environment = {
  production: true,
  baseUrl: 'https://test.meeting-point/service/', // '/api/' not authorized
  hostUrl: 'https://test.meeting-point/',
  authConfig: {
    tokenExpiration: 0 // in days
  }
};
