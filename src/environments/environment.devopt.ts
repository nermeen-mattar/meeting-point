export const environment = {
  production: true,
  baseUrl: 'https://dev.meeting-point/service/', // '/api/' not authorized
  hostUrl: 'https://dev.meeting-point/',
  authConfig: {
    tokenExpiration: 0 // in days
  }
};
